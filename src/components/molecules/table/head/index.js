import TableHeadRoot from './Root';
import TableHeadData from './Data';

const TableHead = {
  Root: TableHeadRoot,
  Data: TableHeadData,
};

export default TableHead;
export {
  TableHead,
  TableHeadRoot,
  TableHeadData,
};