import { ProductDisplay } from '@/components/organisms/blocks';

const TemplatesSingleProduct = ({ id, namespace }) => {
  return (
    <ProductDisplay
      id={id}
      namespace={namespace}
    />
  );
};

export default TemplatesSingleProduct;
