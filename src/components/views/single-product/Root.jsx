import { SingleProductTemplate } from '@/components/templates';

const ViewsSingleProduct = (props) => {
  return (
    <SingleProductTemplate
      namespace='pages.singleProduct'
      {...props}
    />
  );
};

export default ViewsSingleProduct;
