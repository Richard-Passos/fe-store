import { AboutTemplate } from '@/components/templates';

const ViewsAbout = (props) => {
  return (
    <AboutTemplate
      namespace='pages.about'
      {...props}
    />
  );
};

export default ViewsAbout;
