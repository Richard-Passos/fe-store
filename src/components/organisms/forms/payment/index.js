import { Form } from '@/components/molecules';

import PaymentFormRoot from './Root';

const PaymentForm = {
  ...Form,
  Root: PaymentFormRoot
};

export default PaymentForm;
export { PaymentForm, PaymentFormRoot };
