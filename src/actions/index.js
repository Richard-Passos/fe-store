import getCookie from './getCookie';
import setCookie from './setCookie';

const actions = {
  getCookie: getCookie,
  setCookie: setCookie
};

export default actions;
export { actions, getCookie, setCookie };
